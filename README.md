# Homebrew Tap for ndifftool

Homebrew formulae for [ndifftool](https://github.com/openrightorg/ndifftool).

## Installation

```sh
brew tap openrightorg/tap
brew install ndifftool
```

Or install directly with:

```sh
brew install openrightorg/tap/ndifftool
```
